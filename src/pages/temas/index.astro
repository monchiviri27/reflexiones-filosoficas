---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

const temas = await getCollection('temas');
---

<BaseLayout title="Temas filosóficos">
  <div class="max-w-6xl mx-auto">
    <!-- Hero -->
    <div class="mb-12">
      <h1 class="text-4xl font-display font-bold text-filosofia-primario mb-4">
        20 temas filosóficos
      </h1>
      <p class="text-lg text-gray-600">
        Explora los grandes interrogantes humanos a través de estos temas organizados.
        Cada tema incluye obras literarias que lo abordan y reflexiones filosóficas.
      </p>
    </div>
    
    <!-- Grid de temas -->
    {temas.length > 0 ? (
      <div class="grid sm:grid-cols-2 lg:grid-cols-4 gap-6">
        {temas.map(tema => (
          <a 
            href={`/temas/${tema.id}`}
            class="bg-white rounded-xl border border-gray-200 p-6 hover:shadow-lg hover:border-filosofia-acento transition-all group h-full flex flex-col"
            style={`border-left-color: ${tema.data.color}; border-left-width: 4px;`}
          >
            <div class="mb-4">
              <div class="text-3xl mb-2">{tema.data.icono}</div>
              <h3 class="text-xl font-display font-semibold text-filosofia-primario group-hover:text-filosofia-acento mb-2">
                {tema.data.titulo}
              </h3>
            </div>
            
            <p class="text-gray-600 text-sm flex-1 mb-4">
              {tema.data.descripcion}
            </p>
            
            {tema.data.preguntas && tema.data.preguntas.length > 0 && (
              <div class="mt-auto pt-4 border-t border-gray-100">
                <p class="text-xs text-gray-500 mb-1">Preguntas clave:</p>
                <p class="text-sm text-gray-700 font-medium">
                  {tema.data.preguntas[0].substring(0, 60)}...
                </p>
              </div>
            )}
          </a>
        ))}
      </div>
    ) : (
      <div class="text-center py-16 bg-white rounded-lg border">
        <div class="text-6xl mb-4">🧠</div>
        <h3 class="text-xl font-display font-semibold text-gray-700 mb-2">
          Temas en preparación
        </h3>
        <p class="text-gray-600 mb-6">
          Estamos organizando los 20 temas filosóficos principales. 
          Pronto estarán disponibles con obras y análisis relacionados.
        </p>
        <div class="inline-flex flex-wrap gap-2 justify-center">
          {['Tiempo y Eternidad', 'Identidad y Memoria', 'Realidad y Sueño', 'Muerte y Trascendencia', 
            'Vida y Existencia', 'Lenguaje y Significado', 'Soledad y Comunidad', 'Belleza y Arte'].map(tema => (
            <span class="bg-gray-100 text-gray-700 px-3 py-1 rounded-full text-sm">
              {tema}
            </span>
          ))}
        </div>
      </div>
    )}
    
    <!-- Descripción del sistema -->
    <div class="mt-16 bg-filosofia-claro p-8 rounded-2xl">
      <h2 class="text-2xl font-display font-bold text-filosofia-primario mb-4">
        ¿Cómo usar esta sección?
      </h2>
      <div class="grid md:grid-cols-3 gap-6">
        <div class="bg-white p-4 rounded-lg">
          <div class="text-2xl mb-2">1️⃣</div>
          <h3 class="font-semibold text-filosofia-primario mb-2">Elige un tema</h3>
          <p class="text-sm text-gray-600">
            Selecciona uno de los 20 temas filosóficos que más te interese explorar.
          </p>
        </div>
        
        <div class="bg-white p-4 rounded-lg">
          <div class="text-2xl mb-2">2️⃣</div>
          <h3 class="font-semibold text-filosofia-primario mb-2">Lee las obras</h3>
          <p class="text-sm text-gray-600">
            Descubre poemas, cuentos y ensayos que abordan ese tema desde diferentes perspectivas.
          </p>
        </div>
        
        <div class="bg-white p-4 rounded-lg">
          <div class="text-2xl mb-2">3️⃣</div>
          <h3 class="font-semibold text-filosofia-primario mb-2">Reflexiona</h3>
          <p class="text-sm text-gray-600">
            Acompaña cada obra con análisis filosófico y preguntas para tu propia reflexión.
          </p>
        </div>
      </div>
    </div>
  </div>
</BaseLayout>